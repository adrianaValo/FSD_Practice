import React, { useState } from "react";
import { useNavigate } from "react-router-dom";

//py, px= padding

const SearchBar = () => {
  return <div className="container py-4 px-2">SearchBar</div>;
};

export default SearchBar;
